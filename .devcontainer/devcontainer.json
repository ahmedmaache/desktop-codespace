{
  "name": "Desktop Codespace with RDP",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
    "ghcr.io/devcontainers/features/desktop-lite:1": {
      "version": "latest",
      "password": "vscode",
      "webPort": "6080",
      "vncPort": "5901"
    }
  },
  "forwardPorts": [
    3389,
    6080,
    5901
  ],
  "portsAttributes": {
    "3389": {
      "label": "Desktop (RDP)",
      "onAutoForward": "notify"
    },
    "6080": {
      "label": "Desktop (Web/noVNC)",
      "onAutoForward": "silent"
    },
    "5901": {
      "label": "Desktop (VNC)",
      "onAutoForward": "silent"
    }
  },
  "postCreateCommand": "bash -c 'sudo apt-get update && sudo apt-get install -y xrdp xfce4 xfce4-goodies dbus-x11 && sudo sed -i \"s/^port=.*/port=3389/\" /etc/xrdp/xrdp.ini && echo \"xfce4-session\" | tee ~/.xsession && sudo chmod +x ~/.xsession && sudo service xrdp start && echo \"✅ Desktop ready! RDP on 3389, VNC on 5901, Web on 6080. Password: vscode\"'",
  "postStartCommand": "sudo service xrdp start",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode-remote.remote-containers"
      ]
    }
  }
}